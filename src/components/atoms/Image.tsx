"use client";

import styled from "styled-components";

const StyledImage = styled.img`
  max-width: 100%;
  height: 180px;
  object-fit: contain;
  margin-bottom: 12px;
`;

export default StyledImage;
